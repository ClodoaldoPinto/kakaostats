#!/usr/bin/env python

connStr = dict()
connStr["backend"] = \
  "host=localhost dbname=fahstats user=kakaostats password=aroeira"
connStr["frontend"] = \
  "host=localhost dbname=fahstats user=phpUser password=aroeira port=6432"
