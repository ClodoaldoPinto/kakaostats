#!/usr/bin/env python

connStr = dict()

connStr['backend'] = \
  'host=localhost dbname=fahstats user=kakaostats password=somepassword'

connStr['teste'] = \
  'host=localhost dbname=fahstats user=phpUser password=somepassword port=9999'

